r, 0, buffer.Length);
                }
            }
        }

        for (int i = 0; i < parts; i++)
        {
            using (var source = new FileStream(string.Format("../../Part-{0}.txt", i), FileMode.Open))
            {
                using (var destination = new FileStream(assembleP